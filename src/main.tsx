import React from 'react'
import ReactDOM from 'react-dom/client'
import DoraRestaurant from '../dora-restaurant.jsx'

ReactDOM.createRoot(document.getElementById('app')!).render(
  <React.StrictMode>
    <DoraRestaurant />
  </React.StrictMode>,
)
